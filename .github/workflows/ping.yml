name: ping
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/ping.yml'
permissions:
  contents: read
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello-from-ci
